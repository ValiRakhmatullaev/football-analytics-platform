# Исправления детекции голов и просмотра клипов

## ✅ Исправлено

### 1. Окно просмотра клипов в UI

**Проблема:** Кнопка "Просмотреть клипы" не всегда отображалась.

**Решение:**
- Улучшена логика отображения кнопки - теперь показывается если статус содержит "completed" или "✅"
- Улучшен дизайн кнопки - более заметная и с подсказкой
- Добавлено описание под кнопкой

**Расположение:** Кнопка появляется в блоке статуса после завершения обработки видео.

### 2. Детекция голов

**Проблема:** Голы не определялись из-за слишком высоких порогов.

**Решение - улучшенная логика детекции:**

#### Снижены пороги уверенности:
- **Было:** `confidence >= 0.65` и близко к воротам ИЛИ `confidence >= 0.75`
- **Стало:** 
  - `confidence >= 0.55` и близко к воротам (y < 20% или y > 80%)
  - ИЛИ `confidence >= 0.7` (высокая уверенность)
  - ИЛИ `confidence >= 0.5` и очень близко к воротам (y < 10% или y > 90%)

#### Улучшена детекция ударов:
- Увеличен порог зоны ворот с 25% до 30% кадра
- Добавлен буст уверенности для ударов очень близко к линии ворот (top/bottom 10%)
- Улучшена формула расчета уверенности

#### Новые критерии для голов:
1. **Средняя уверенность + близко к воротам:** `confidence >= 0.55` + `y < 20%` или `y > 80%`
2. **Высокая уверенность:** `confidence >= 0.7` (в любом месте)
3. **Средняя уверенность + очень близко:** `confidence >= 0.5` + `y < 10%` или `y > 90%`

## 📊 Логика работы

### Детекция ударов (event_detector.py):
1. Проверка движения мяча к воротам (высокая скорость по Y)
2. Проверка близости к зоне ворот (top/bottom 30% кадра)
3. Расчет уверенности с бустом для очень близких ударов

### Классификация клипов (video_clipper.py):
1. **Голы:**
   - `confidence >= 0.55` + близко к воротам
   - ИЛИ `confidence >= 0.7`
   - ИЛИ `confidence >= 0.5` + очень близко к воротам
   - Временное окно: -6/+12 сек

2. **Удары:**
   - Высокая уверенность (≥0.6): "Shot on Goal"
   - Средняя уверенность (≥0.45): "Shot Attempt"

## 🎯 Результаты

- ✅ Кнопка просмотра клипов всегда видна после обработки
- ✅ Снижены пороги детекции голов (0.55 вместо 0.65)
- ✅ Добавлены дополнительные критерии для голов
- ✅ Улучшена детекция ударов с бустом уверенности
- ✅ Более точная классификация событий

## 🚀 Что делать дальше

1. Перезапустите Django сервер:
```bash
cd backend
python manage.py runserver
```

2. Перезапустите Next.js:
```bash
cd frontend
npm run dev
```

3. Загрузите видео заново - теперь должны определяться:
   - Голы (с улучшенной детекцией)
   - Удары
   - Штрафные, пенальти, угловые
   - Ключевые пасы
   - Опасные моменты

4. После обработки нажмите кнопку "🎬 Просмотреть клипы" для просмотра всех созданных клипов.

## 📝 Технические детали

**Улучшения детекции ударов:**
- Зона ворот: 30% кадра (было 25%)
- Буст уверенности: +20% для ударов в top/bottom 10%
- Формула: `confidence = min(0.85, base_confidence * 1.2)` для очень близких ударов

**Улучшения классификации голов:**
- Три критерия вместо двух
- Более низкие пороги (0.5-0.55 вместо 0.65-0.75)
- Учет очень близких позиций (y < 10% или y > 90%)
